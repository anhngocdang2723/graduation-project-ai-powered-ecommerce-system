# 🎨 Visual Architecture - Recommendation System

## 📦 Docker Containers Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                    Docker Network: app_network                      │
│                                                                     │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐            │
│  │  PostgreSQL  │  │    Redis     │  │   pgAdmin    │            │
│  │   :5432      │  │    :6379     │  │    :5050     │            │
│  │  (Database)  │  │   (Cache)    │  │  (DB UI)     │            │
│  └──────┬───────┘  └──────┬───────┘  └──────────────┘            │
│         │                  │                                       │
│         │ ┌────────────────┴──────────────────┐                  │
│         │ │                                   │                  │
│  ┌──────┴─▼────────┐  ┌────────────┐  ┌──────▼──────────┐       │
│  │  Medusa Backend │  │  Chatbot   │  │ Recommendation  │       │
│  │     :9000       │  │   :8000    │  │     :8001       │       │
│  │   (Node.js)     │  │  (Python)  │  │    (Python)     │       │
│  └─────────────────┘  └────────────┘  └─────────────────┘       │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
          ▲                    ▲                    ▲
          │                    │                    │
    Port 9000              Port 8000            Port 8001
          │                    │                    │
          └────────────────────┴────────────────────┘
                               │
                    ┌──────────▼──────────┐
                    │  Vercel Commerce    │
                    │   (Frontend)        │
                    │   localhost:3000    │
                    └─────────────────────┘
```

## 🗄️ Database Schema Layout

```
┌──────────────────────────────────────────────────────────────────┐
│                  PostgreSQL: medusa-store                        │
│                                                                  │
│  Schema: public (DEFAULT - All tables here!)                    │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                                                            │ │
│  │  ┌─────────────────┐  ┌──────────────────┐              │ │
│  │  │  Medusa Tables  │  │  Chatbot Tables  │              │ │
│  │  ├─────────────────┤  ├──────────────────┤              │ │
│  │  │ • product       │  │ • chatbot_context │             │ │
│  │  │ • cart          │  │ • chatbot_resp... │             │ │
│  │  │ • order         │  │ • chatbot_analytics│            │ │
│  │  │ • customer      │  └──────────────────┘              │ │
│  │  │ • product_type  │                                     │ │
│  │  │ • region        │  ┌──────────────────────────────┐  │ │
│  │  │ • ...           │  │  Recommendation Tables ⭐    │  │ │
│  │  └─────────────────┘  ├──────────────────────────────┤  │ │
│  │                       │ • rec_user_interactions      │  │ │
│  │                       │ • rec_user_preferences       │  │ │
│  │                       │ • rec_product_similarities   │  │ │
│  │                       │ • rec_frequently_together    │  │ │
│  │                       │ • rec_recommendations_cache  │  │ │
│  │                       │ • rec_user_segments          │  │ │
│  │                       │ • rec_analytics              │  │ │
│  │                       └──────────────────────────────┘  │ │
│  │                                                          │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                │
│  Tất cả tables cùng schema "public"!                          │
│  Không có schema riêng cho recommendation                     │
└────────────────────────────────────────────────────────────────┘
```

## 🔄 Request Flow: Track User Interaction

```
┌─────────────┐
│   User      │ 1. Click vào product
│   Browser   │
└──────┬──────┘
       │
       │ 2. Component auto track
       ▼
┌─────────────────────────────────────────┐
│  vercel-commerce/components/tracking/   │
│  tracking-wrapper.tsx                   │
│                                         │
│  useEffect(() => {                      │
│    trackInteraction({                   │
│      productId: product.id,             │
│      interactionType: 'view'            │
│    })                                   │
│  })                                     │
└──────┬──────────────────────────────────┘
       │
       │ 3. POST /api/recommendations/track
       ▼
┌─────────────────────────────────────────┐
│  vercel-commerce/app/api/               │
│  recommendations/track/route.ts         │
│                                         │
│  export async function POST(req) {      │
│    const body = await req.json()        │
│    await fetch(                         │
│      'http://recommendation:8001/track',│
│      { body }                           │
│    )                                    │
│  }                                      │
└──────┬──────────────────────────────────┘
       │
       │ 4. Forward to Python service
       │    (Docker network: recommendation:8001)
       ▼
┌─────────────────────────────────────────┐
│  recommendation-service/app/main.py     │
│                                         │
│  @app.post("/track")                    │
│  async def track(request):              │
│    tracker = InteractionTracker(db)     │
│    await tracker.track(                 │
│      user_id, product_id, ...           │
│    )                                    │
│  }                                      │
└──────┬──────────────────────────────────┘
       │
       │ 5. Insert to database
       ▼
┌─────────────────────────────────────────┐
│  recommendation-service/app/services/   │
│  interaction_tracker.py                 │
│                                         │
│  await conn.execute("""                 │
│    INSERT INTO rec_user_interactions    │
│    (id, user_id, product_id, ...)       │
│    VALUES ($1, $2, $3, ...)             │
│  """)                                   │
└──────┬──────────────────────────────────┘
       │
       │ 6. SQL INSERT via asyncpg
       ▼
┌─────────────────────────────────────────┐
│  PostgreSQL Container                   │
│  Database: medusa-store                 │
│  Schema: public                         │
│                                         │
│  Table: rec_user_interactions           │
│  ┌───────────────────────────────────┐  │
│  │ id    | user_id | product_id | ...│  │
│  │ int_x | user123 | prod_abc   | ...│  │
│  └───────────────────────────────────┘  │
└──────┬──────────────────────────────────┘
       │
       │ 7. Auto update preferences
       ▼
┌─────────────────────────────────────────┐
│  Table: rec_user_preferences            │
│  ┌───────────────────────────────────┐  │
│  │ user_id | category_scores | ...   │  │
│  │ user123 | {"backpacks": 1.0}| ... │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

## 🎯 Request Flow: Get Recommendations

```
┌─────────────┐
│   User      │ 1. Vào homepage
│   Browser   │
└──────┬──────┘
       │
       │ 2. Component fetch recommendations
       ▼
┌─────────────────────────────────────────┐
│  vercel-commerce/components/            │
│  recommendations/personalized.tsx       │
│                                         │
│  const recs = await fetch(              │
│    '/api/recommendations?userId=xxx'    │
│  )                                      │
└──────┬──────────────────────────────────┘
       │
       │ 3. GET /api/recommendations
       ▼
┌─────────────────────────────────────────┐
│  vercel-commerce/app/api/               │
│  recommendations/route.ts               │
│                                         │
│  const response = await fetch(          │
│    'http://recommendation:8001/...'    │
│  )                                      │
└──────┬──────────────────────────────────┘
       │
       │ 4. Forward to Python service
       ▼
┌─────────────────────────────────────────┐
│  recommendation-service/app/main.py     │
│                                         │
│  @app.get("/recommendations")           │
│  async def get_recommendations():       │
│    engine = RecommendationEngine(db)    │
│    return await engine.get_personalized │
│  }                                      │
└──────┬──────────────────────────────────┘
       │
       │ 5. Check Redis cache
       ▼
┌─────────────────────────────────────────┐
│  Redis Container                        │
│  Database: 1 (recommendation cache)     │
│                                         │
│  Key: rec:recommendations:user123       │
│  Value: [product1, product2, ...]       │
│  TTL: 3600 seconds                      │
└──────┬──────────────────────────────────┘
       │
       │ 6. If cache miss → Query database
       ▼
┌─────────────────────────────────────────┐
│  recommendation-service/app/services/   │
│  recommendation_engine.py               │
│                                         │
│  # Get user preferences                 │
│  prefs = await get_user_preferences()   │
│                                         │
│  # Get recent interactions              │
│  recent = await get_recent_products()   │
│                                         │
│  # Hybrid algorithm                     │
│  content_recs = content_based(prefs)    │
│  collab_recs = collaborative(user)      │
│                                         │
│  score = 0.4*content + 0.6*collab       │
└──────┬──────────────────────────────────┘
       │
       │ 7. Multiple database queries
       ▼
┌─────────────────────────────────────────┐
│  PostgreSQL - Multiple Tables           │
│                                         │
│  Query 1: rec_user_preferences          │
│  ┌────────────────────────────────────┐ │
│  │ Get category scores for user       │ │
│  └────────────────────────────────────┘ │
│                                         │
│  Query 2: rec_user_interactions         │
│  ┌────────────────────────────────────┐ │
│  │ Get recent viewed products         │ │
│  └────────────────────────────────────┘ │
│                                         │
│  Query 3: rec_product_similarities      │
│  ┌────────────────────────────────────┐ │
│  │ Get similar products               │ │
│  └────────────────────────────────────┘ │
│                                         │
│  Query 4: product (Medusa table)        │
│  ┌────────────────────────────────────┐ │
│  │ Get product details (title, img)   │ │
│  └────────────────────────────────────┘ │
└──────┬──────────────────────────────────┘
       │
       │ 8. ML Processing
       ▼
┌─────────────────────────────────────────┐
│  scikit-learn / numpy                   │
│                                         │
│  • Calculate content similarity         │
│  • Calculate collaborative filtering    │
│  • Combine scores (weighted)            │
│  • Rank products                        │
│  • Select top N                         │
└──────┬──────────────────────────────────┘
       │
       │ 9. Cache result
       ▼
┌─────────────────────────────────────────┐
│  Redis - Store for 1 hour               │
│  SET rec:recommendations:user123        │
│      [product_list]                     │
│      EX 3600                            │
└──────┬──────────────────────────────────┘
       │
       │ 10. Return JSON
       ▼
┌─────────────────────────────────────────┐
│  Response to Frontend                   │
│  {                                      │
│    "recommendations": [                 │
│      {                                  │
│        "id": "prod_123",                │
│        "title": "Backpack",             │
│        "thumbnail": "url",              │
│        "score": 0.95                    │
│      }                                  │
│    ],                                   │
│    "algorithm": "hybrid",               │
│    "cached": false                      │
│  }                                      │
└──────┬──────────────────────────────────┘
       │
       │ 11. Display on page
       ▼
┌─────────────┐
│   User      │ Thấy personalized products
│   Browser   │
└─────────────┘
```

## 🏗️ File Structure & Deployment

```
graduation-project/
│
├── 📄 docker-compose.yml  ⭐ MASTER FILE
│   └── Defines ALL services:
│       • postgres (shared database)
│       • redis (shared cache)
│       • medusa (backend)
│       • chatbot (AI)
│       • chatbot_worker
│       • recommendation (ML) ⭐ NEW
│       • pgadmin (UI)
│
├── 📁 my-medusa-store/
│   ├── Dockerfile
│   └── src/  (Node.js code)
│
├── 📁 chatbot-service/
│   ├── Dockerfile
│   └── app/  (Python code)
│
└── 📁 recommendation-service/  ⭐ NEW
    ├── Dockerfile
    ├── init_db.sh  ⭐ Auto runs on startup
    ├── app/
    │   ├── main.py (FastAPI)
    │   ├── config.py
    │   └── services/
    │       ├── recommendation_engine.py
    │       └── interaction_tracker.py
    └── database/
        └── init_schema.sql  ⭐ Creates 7 tables
```

## 🚀 Startup Sequence

```
Step 1: Build
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
$ docker-compose build

Building: medusa           [████████████] 100%
Building: chatbot          [████████████] 100%
Building: recommendation   [████████████] 100% ⭐


Step 2: Start Infrastructure
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
$ docker-compose up -d postgres redis

✓ postgres    Started
✓ redis       Started

Wait 10 seconds...


Step 3: Start Backend
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
$ docker-compose up -d medusa

✓ medusa      Started
  └─ Connecting to postgres...
  └─ Running migrations...
  └─ Ready on port 9000

Wait 20 seconds...


Step 4: Start AI Services
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
$ docker-compose up -d chatbot recommendation

✓ chatbot              Started
  └─ Ready on port 8000

✓ recommendation       Started ⭐
  ├─ init_db.sh running...
  ├─ Waiting for PostgreSQL...
  ├─ PostgreSQL ready!
  ├─ Creating tables...
  │  ├─ rec_user_interactions      ✓
  │  ├─ rec_user_preferences       ✓
  │  ├─ rec_product_similarities   ✓
  │  ├─ rec_frequently_together    ✓
  │  ├─ rec_recommendations_cache  ✓
  │  ├─ rec_user_segments          ✓
  │  └─ rec_analytics              ✓
  └─ FastAPI server started on port 8001


Step 5: Verify
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
$ curl http://localhost:8001/health

{"status":"healthy","service":"recommendation"} ✓


ALL SERVICES READY! 🎉
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Port 5432: PostgreSQL      ✓
Port 6379: Redis           ✓
Port 9000: Medusa          ✓
Port 8000: Chatbot         ✓
Port 8001: Recommendation  ✓ NEW
Port 5050: pgAdmin         ✓
```

## 📊 pgAdmin Navigation

```
pgAdmin UI (http://localhost:5050)
│
├─ 🔐 Login
│  Email: admin@admin.com
│  Password: admin
│
└─ 📁 Servers
   └─ 📁 medusa-postgres (auto-configured)
      └─ 📁 Databases
         └─ 📁 medusa-store  ⭐ Main database
            └─ 📁 Schemas
               └─ 📁 public  ⭐ All tables here!
                  ├─ 📁 Tables
                  │  ├─ 📄 cart
                  │  ├─ 📄 customer
                  │  ├─ 📄 order
                  │  ├─ 📄 product
                  │  ├─ 📄 rec_analytics              ⭐
                  │  ├─ 📄 rec_frequently_together    ⭐
                  │  ├─ 📄 rec_product_similarities   ⭐
                  │  ├─ 📄 rec_recommendations_cache  ⭐
                  │  ├─ 📄 rec_user_interactions      ⭐
                  │  ├─ 📄 rec_user_preferences       ⭐
                  │  ├─ 📄 rec_user_segments          ⭐
                  │  └─ ... (50+ total tables)
                  │
                  └─ 📁 Views
```

## 🎯 Summary

```
┌────────────────────────────────────────────────────┐
│  ✓ Tất cả services trong 1 docker-compose.yml     │
│  ✓ Database được share giữa các services          │
│  ✓ Tables trong public schema (không riêng)       │
│  ✓ Auto initialization khi container start        │
│  ✓ Network internal communication via names       │
│  ✓ Ports exposed cho external access              │
└────────────────────────────────────────────────────┘
```
